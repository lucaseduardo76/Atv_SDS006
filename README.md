### Pra rodar as telas

  `npm i` para instalar as dependências.
  `npm run dev` pra iniciar o servidor.

  Caso queira trocar de tela, basta ir em '/src/main.tsx' e trocar o container
  pela tela que desejam ver, porque atualmente não tem nada configurado ainda...
  